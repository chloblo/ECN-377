#this is an R script with all the running code

x<-c(2,1,3) #naming a vector c() is a vector
mean(x)# will find the mean of the named vector
mean()# also can be written as sum()/length()
sqrt() #square root
<- #assinging something a value
$ #pulls the variable out of the data set--- ex) wage 1 $ wage #pulls wage out
summary() #quick snapshot of every variable in a data set    ex)min, mean, max
var() #sample variance
cov() #measures whether two variables tend to move together and in which dir
cor ()#sample correlation--how to variables move together and in what direction
sec() #creates a sequence of numbers
: #a colon creates a sequence (e.g. 1:6 for a die
rep() #repeats a value
sum()#adds all the values together 
sum(x*probability)#E(X) finds the expected value